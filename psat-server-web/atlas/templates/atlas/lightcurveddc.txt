#obs,ra,dec,x,y,mag,dmag,mag5sig,filt,magzp,texp,mjd,major,minor,phi,det,chin,pvr,ptr,pmv,pkn,pno,pbn,pcr,pxt,psc,dup,wpflx,dflx,obj,det_id
{% for row in t.rows %}{{ row.obs }},{{ row.ra }},{{ row.dec }},{{ row.x }},{{ row.y }},{{ row.mag }},{{ row.dmag }},{{ row.mag5sig }},{{ row.filt }},{{ row.magzp }},{{ row.texp }},{{ row.mjd }},{{ row.major }},{{ row.minor }},{{ row.phi }},{{ row.det }},{{ row.chin }},{{ row.pvr }},{{ row.ptr }},{{ row.pmv }},{{ row.pkn }},{{ row.pno }},{{ row.pbn }},{{ row.pcr }},{{ row.pxt }},{{ row.psc }},{{ row.dup }},{{ row.wpflx }},{{ row.dflx }},{{ row.obj }},{{ row.det_id }}
{% endfor %}
